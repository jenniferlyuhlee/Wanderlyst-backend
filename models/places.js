"use strict";
/** Location Model */

const db = require("../db");
const { BadRequestError } = require("../expressError");

/** Class Place with db query methods for all places. */
class Place{

    /** Creates new location entry
     * Returns location obj
     * 
     */
}

module.exports = Place;